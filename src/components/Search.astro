---
// Pagefind Search Component
---

<link rel="stylesheet" href="/pagefind/pagefind-ui.css" />
<div id="search" class="search-container"></div>
<script src="/pagefind/pagefind-ui.js" defer></script>
<script is:inline>
  window.addEventListener('DOMContentLoaded', function() {
    if (typeof PagefindUI !== 'undefined') {
      new PagefindUI({
        element: "#search",
        showSubResults: true,
        showImages: false,
        translations: {
          placeholder: "Search...",
          zero_results: "Nothing found"
        }
      });
    }
  });
</script>

<style is:global>
  /* Search container */
  .search-container {
    position: relative;
    min-width: 44px;
  }

  /* Pagefind UI variables */
  .search-container {
    --pagefind-ui-scale: 0.8;
    --pagefind-ui-primary: #8b5cf6;
    --pagefind-ui-text: #1e1b4b;
    --pagefind-ui-background: white;
    --pagefind-ui-border: #2d1f4a;
    --pagefind-ui-border-width: 1px;
    --pagefind-ui-border-radius: 8px;
  }

  /* Hide the form wrapper default styles */
  .search-container .pagefind-ui__form {
    position: relative;
  }

  /* Style the search input for dark header */
  .search-container .pagefind-ui__search-input {
    background-color: rgba(255, 255, 255, 0.15) !important;
    color: white !important;
    border: 1px solid rgba(255, 255, 255, 0.3) !important;
    min-width: 120px;
    width: 120px;
  }

  .search-container .pagefind-ui__search-input:focus {
    width: 200px;
    background-color: rgba(255, 255, 255, 0.25) !important;
  }

  .search-container .pagefind-ui__search-input::placeholder {
    color: rgba(255, 255, 255, 0.7) !important;
  }

  /* Search button/icon */
  .search-container .pagefind-ui__search-clear {
    color: white !important;
  }

  /* Results dropdown */
  .search-container .pagefind-ui__drawer {
    position: absolute;
    right: 0;
    top: 100%;
    margin-top: 0.5rem;
    background: white !important;
    border-radius: 0.5rem;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
    min-width: 300px;
    max-width: 400px;
    max-height: 70vh;
    overflow-y: auto;
    z-index: 1000;
  }

  .search-container .pagefind-ui__result-title {
    color: #1e1b4b;
    font-weight: 600;
  }

  .search-container .pagefind-ui__result-link:hover {
    background-color: #f5f6ff;
  }

  /* Mobile adjustments */
  @media (max-width: 768px) {
    .search-container .pagefind-ui__search-input {
      width: 100px;
      min-width: 100px;
    }

    .search-container .pagefind-ui__search-input:focus {
      width: 140px;
    }

    .search-container .pagefind-ui__drawer {
      position: fixed;
      left: 1rem;
      right: 1rem;
      top: 60px;
      min-width: auto;
      max-width: none;
    }
  }
</style>
