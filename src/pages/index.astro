---
import BaseLayout from '../layouts/BaseLayout.astro';
import OptimizedImage from '../components/OptimizedImage.astro';
import Hero from '../components/Hero.astro';
import CategoryCard from '../components/CategoryCard.astro';
import { getCollection } from 'astro:content';
import { SITE, CATEGORIES } from '../config';

// Get latest blog posts
const posts = await getCollection('blog', ({ data }) => !data.draft);
const latestPosts = posts
  .sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
  .slice(0, 3);
---

<BaseLayout title="Turn Boring Work Hours Into Career Growth">
  <Hero
    title="Turn Boring Work Hours Into Career Growth"
    description="Discover how to learn AI skills, earn certifications, and boost your career — all while you're stuck at your desk with nothing to do."
    ctaText="See 25 Productive Ideas →"
    ctaHref="/productive-things-bored-at-work/"
  />

  <!-- Featured Article -->
  <section class="py-24 bg-background-alt relative overflow-hidden">
    <div class="absolute top-0 right-0 w-1/4 h-full bg-accent/5 -skew-x-12 transform origin-top"></div>
    <div class="max-w-4xl mx-auto px-4 relative z-10">
      <div class="flex items-center gap-2 mb-4">
        <span class="w-8 h-px bg-accent"></span>
        <span class="text-accent font-bold text-xs uppercase tracking-[0.2em]">Essential Reading</span>
      </div>
      <h2 class="text-3xl md:text-5xl font-bold mt-2 mb-6 leading-tight">
        25 Productive Things to Do When Bored at Work
      </h2>
      <p class="text-xl text-text-light mb-8 leading-relaxed italic border-l-4 border-primary/10 pl-6">
        "Stop scrolling social media. Start building skills that actually move the needle."
      </p>
      <div class="grid sm:grid-cols-3 gap-6 mb-10">
        <div class="bg-white p-6 rounded-2xl shadow-sm border border-primary/5">
          <div class="text-accent text-xl mb-2">⚡</div>
          <p class="text-sm font-medium">Free courses you can finish today</p>
        </div>
        <div class="bg-white p-6 rounded-2xl shadow-sm border border-primary/5">
          <div class="text-accent text-xl mb-2">💼</div>
          <p class="text-sm font-medium">High-demand skills for 2026</p>
        </div>
        <div class="bg-white p-6 rounded-2xl shadow-sm border border-primary/5">
          <div class="text-accent text-xl mb-2">🕵️</div>
          <p class="text-sm font-medium">Learn while looking busy</p>
        </div>
      </div>
      <a href="/productive-things-bored-at-work/" class="btn-secondary group">
        Read the Full Guide <span class="inline-block transition-transform group-hover:translate-x-1">→</span>
      </a>
    </div>
  </section>

  <!-- Content Categories -->
  <section class="py-16">
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid md:grid-cols-3 gap-8">
        {CATEGORIES.map(category => (
          <CategoryCard {...category} />
        ))}
      </div>
    </div>
  </section>

  <!-- Why AI Section -->
  <section class="py-24 bg-primary text-white relative overflow-hidden">
    <div class="absolute inset-0 opacity-10 bg-[radial-gradient(#3b82f6_1px,transparent_1px)] [background-size:24px_24px]"></div>
    <div class="max-w-4xl mx-auto px-4 text-center relative z-10">
      <h2 class="text-3xl md:text-4xl font-bold mb-12 text-white">The "Oh No" Stats</h2>
      <div class="grid md:grid-cols-3 gap-12 mb-12">
        <div class="p-4">
          <div class="text-5xl md:text-6xl font-bold text-accent mb-4">77%</div>
          <p class="text-white/60 text-sm uppercase tracking-wider font-semibold">of companies are already on the AI train</p>
        </div>
        <div class="p-4 border-y md:border-y-0 md:border-x border-white/10">
          <div class="text-5xl md:text-6xl font-bold text-accent mb-4">40%</div>
          <p class="text-white/60 text-sm uppercase tracking-wider font-semibold">of us will need new skills by 2030</p>
        </div>
        <div class="p-4">
          <div class="text-5xl md:text-6xl font-bold text-accent mb-4">$25K</div>
          <p class="text-white/60 text-sm uppercase tracking-wider font-semibold">potential salary bump for AI mastery</p>
        </div>
      </div>
      <p class="text-white/70 max-w-2xl mx-auto text-lg leading-relaxed">
        The pros who learn AI now will have a massive edge. If you're bored at home-office anyway, why not future-proof your career while getting paid?
      </p>
    </div>
  </section>

  <!-- Latest Articles -->
  {latestPosts.length > 0 && (
    <section class="py-16">
      <div class="max-w-6xl mx-auto px-4">
        <h2 class="text-2xl md:text-3xl font-bold mb-2">Latest Articles</h2>
        <p class="text-text-light mb-8">Fresh guides and tutorials to help you make the most of your work hours.</p>
        <div class="grid md:grid-cols-3 gap-6">
          {latestPosts.map(post => (
            <article class="bg-white rounded-xl overflow-hidden shadow-sm border border-primary/5 hover:shadow-md transition-shadow">
              {post.data.image && (
                <OptimizedImage
                  src={post.data.image}
                  alt={post.data.title}
                  class="w-full h-48 object-cover"
                />
              )}
              <div class="p-5">
                <h3 class="font-bold mb-2 line-clamp-2">
                  <a href={`/${post.id}/`} class="hover:text-accent transition-colors text-primary">
                    {post.data.title}
                  </a>
                </h3>
                <p class="text-text-light text-sm line-clamp-2 mb-3">
                  {post.data.description}
                </p>
                <a href={`/${post.id}/`} class="text-accent text-sm font-medium hover:underline">
                  Read more →
                </a>
              </div>
            </article>
          ))}
        </div>
        <div class="text-center mt-8">
          <a href="/blog/" class="btn-secondary">
            View All Articles
          </a>
        </div>
      </div>
    </section>
  )}

  <!-- About Section -->
  <section class="py-16 bg-background-alt">
    <div class="max-w-3xl mx-auto px-4">
      <h2 class="text-2xl md:text-3xl font-bold mb-4">About {SITE.title}</h2>
      <p class="text-text-light mb-4 leading-relaxed">
        We've all been there — staring at the clock, waiting for 5pm, mindlessly scrolling through the same websites.
        But what if you could turn those boring hours into something that actually moves your career forward?
      </p>
      <p class="text-text-light mb-4 leading-relaxed">
        {SITE.title} is your guide to productive procrastination. We find the best free and affordable courses,
        tutorials, and certifications you can complete during work downtime. No fluff, no filler — just practical
        resources that help you learn real skills while you're getting paid.
      </p>
      <p class="text-text-light leading-relaxed">
        <strong class="text-primary">Our focus:</strong> AI and tech skills that are in high demand right now.
        The kind of skills that get you noticed, promoted, and paid more.
      </p>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="py-16 bg-gradient-to-r from-accent to-accent-hover text-white">
    <div class="max-w-2xl mx-auto px-4 text-center">
      <h2 class="text-2xl md:text-3xl font-bold mb-4 text-white">Ready to Stop Being Bored?</h2>
      <p class="text-white/90 mb-6">
        Start with our most popular guide and discover 25 productive ways to use your work downtime.
      </p>
      <a href="/productive-things-bored-at-work/" class="inline-block bg-white text-accent px-8 py-3 rounded-lg font-semibold hover:bg-gray-100 transition-colors">
        Get Started →
      </a>
    </div>
  </section>
</BaseLayout>
